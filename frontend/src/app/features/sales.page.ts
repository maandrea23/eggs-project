import { Component, OnInit, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { FarmApiService, Sale } from '../core/farm-api.service';
const today=new Date().toISOString().slice(0,10);
@Component({selector:'app-sales',imports:[FormsModule],template:`<section><p class="eyebrow">COMERCIAL</p><h1>Ventas de huevos</h1><div class="layout"><form (ngSubmit)="save()"><label>Fecha<input type="date" [(ngModel)]="form.date" name="date"></label><label>Cubetas<input type="number" [(ngModel)]="form.cartons" name="cartons" min="1"></label><label>Tipo<select [(ngModel)]="form.cartonType" name="type">@for(type of types;track type){<option [value]="type">{{type === 'UNCLASSIFIED' ? 'Sin clasificar' : type}}</option>}</select></label><label>Precio por cubeta<input type="number" [(ngModel)]="form.pricePerCartonCop" name="price"></label><label>Cliente<input [(ngModel)]="form.customerName" name="customer"></label><label>Teléfono<input [(ngModel)]="form.customerPhone" name="phone"></label><label>Lugar de compra<input [(ngModel)]="form.purchaseLocation" name="location"></label>@if(message()){<p>{{message()}}</p>}<button>Guardar venta</button></form><article><h2>Ventas recientes</h2>@for(sale of sales();track sale.id){<div><strong>{{sale.date}} · {{sale.cartons}} cubetas {{sale.cartonType}}</strong><span>{{sale.customerName}} · $ {{sale.pricePerCartonCop}}</span></div>}@empty{<p>No hay ventas.</p>}</article></div></section>`,styles:`.eyebrow{color:#6f845f;font-weight:800;letter-spacing:.1em}.layout{display:grid;gap:1rem;grid-template-columns:1fr 1fr}form,article{background:#fff;border:1px solid #e2dfd2;border-radius:1.25rem;display:grid;gap:.8rem;padding:1.25rem}label{display:grid;gap:.35rem;font-weight:700}input,select{border:1px solid #ccd4c5;border-radius:.6rem;font:inherit;padding:.65rem}button{background:#496547;border:0;border-radius:999px;color:#fff;font-weight:800;padding:.85rem}article div{border-bottom:1px solid #eee8dc;display:grid;gap:.3rem;padding:.8rem 0}@media(max-width:700px){.layout{grid-template-columns:1fr}}`})
export class SalesPage implements OnInit{types=['C','B','A','AA','AAA','JUMBO','UNCLASSIFIED'];form={date:today,cartons:1,cartonType:'UNCLASSIFIED',pricePerCartonCop:0,customerName:'',customerPhone:'',purchaseLocation:''};readonly sales=signal<Sale[]>([]);readonly message=signal('');constructor(private readonly api:FarmApiService){}ngOnInit(){this.load();}load(){this.api.sales().subscribe(sales=>this.sales.set(sales));}save(){this.api.createSale(this.form).subscribe({next:()=>{this.message.set('Venta registrada y descontada del inventario.');this.load();},error:response=>this.message.set(response.error?.error??'No se pudo guardar la venta.')});}}
