import { Component, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { Router } from '@angular/router';
import { AuthService } from '../core/auth.service';

@Component({ selector: 'app-login', imports: [FormsModule], template: `<section class="login"><form (ngSubmit)="submit()"><p class="eyebrow">BRIANNA EGGS</p><h1>Acceso a la granja</h1><label>Usuario<input [(ngModel)]="username" name="username" required></label><label>Contraseña<input [(ngModel)]="password" name="password" type="password" required></label>@if(error()){<p class="error">{{error()}}</p>}<button [disabled]="loading()">{{loading() ? 'Ingresando…' : 'Ingresar'}}</button><p class="hint">Operador: recolección diaria. Administrador: producción, ventas y gastos. Dueño: control total.</p></form></section>`, styles:`.login{align-items:center;background:linear-gradient(140deg,#425c46,#d6d7ae);display:flex;justify-content:center;min-height:100vh;padding:1rem}form{background:#fffdf8;border-radius:1.5rem;box-shadow:0 18px 40px #0003;display:grid;gap:1rem;max-width:390px;padding:2rem;width:100%}.eyebrow{color:#697f54;font-weight:800;letter-spacing:.12em;margin:0}h1{margin:0}label{display:grid;gap:.4rem;font-weight:700}input{border:1px solid #cbd2bb;border-radius:.7rem;font:inherit;padding:.8rem}button{background:#496547;border:0;border-radius:999px;color:#fff;cursor:pointer;font-weight:800;padding:.9rem}.error{color:#a63232;margin:0}.hint{color:#647067;font-size:.85rem;line-height:1.45;margin:0}` })
export class LoginPage { username='';password='';readonly error=signal('');readonly loading=signal(false);constructor(private readonly auth:AuthService,private readonly router:Router){}submit(){this.loading.set(true);this.error.set('');this.auth.login(this.username,this.password).subscribe({next:()=>void this.router.navigateByUrl('/'),error:()=>{this.error.set('Usuario o contraseña incorrectos.');this.loading.set(false);}});} }
